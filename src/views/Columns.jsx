import React from 'react'
import { opinion } from '../data'
import SingleSection from '../components/SingleSection'

const Columns = () => {
  return <SingleSection name="Columna" array={opinion} />
}

export default Columns

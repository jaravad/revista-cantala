import React from 'react'
import { interviews } from '../data'
import SingleSection from '../components/SingleSection'

const Interviews = () => {
  return <SingleSection name="Entrevistas" array={interviews} />
}

export default Interviews
